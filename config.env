# =============================================================================
# Astroneer VM Configuration
# Edit these values before running setup.sh
# =============================================================================

# --- VM Resources ---
VM_NAME="astroneer-win"
VM_RAM=3072          # MB — Server Core ~1.5GB + Astroneer ~1.5GB
VM_CPUS=2
VM_DISK_SIZE=40      # GB — Server Core ~8GB + Astroneer ~3GB + headroom

# --- VM Network ---
VM_IP="192.168.122.100"

# --- Access ---
VNC_PORT=5900
VNC_PASSWORD="astro123"

# --- Windows Account ---
WIN_USERNAME="astro"
WIN_PASSWORD="astro123"

# --- Astroneer Server ---
# IMPORTANT: ASTRO_PORT must match your playit.gg allocated public port!
# The server registers with the Astroneer backend using PublicIP:Port.
# If these don't match the publicly reachable address, clients see "offline".
ASTRO_PORT=23787
ASTRO_SERVER_NAME="Vertex"
ASTRO_OWNER_NAME="BSoDium"
ASTRO_PUBLIC_IP="147.185.221.181"  # Your playit.gg allocated IP

# --- Paths ---
IMAGES_DIR="/var/lib/libvirt/images"
