# =============================================================================
# Astroneer VM Configuration
# Copy this file to .env and edit the values before running setup.sh:
#   cp .env.example .env
# =============================================================================

# --- VM Resources ---
VM_NAME="astroneer-win"
VM_RAM=4096          # MB — Desktop Experience ~2.5GB + Astroneer ~1.5GB
VM_CPUS=2
VM_DISK_SIZE=40      # GB — Server Core ~8GB + Astroneer ~3GB + headroom

# --- VM Network ---
VM_IP="192.168.122.100"

# --- Access ---
VNC_PORT=5900
VNC_PASSWORD="changeme"

# --- Windows Account ---
WIN_USERNAME="astro"
WIN_PASSWORD="changeme"

# --- Astroneer Server ---
# IMPORTANT: ASTRO_PORT must match your playit.gg allocated public port!
# The server registers with the Astroneer backend using PublicIP:Port.
# If these don't match the publicly reachable address, clients see "offline".
ASTRO_PORT=23787
ASTRO_PUBLIC_IP="0.0.0.0"     # Your playit.gg allocated IP
ASTRO_LAUNCHER_PORT=5000       # AstroLauncher web UI port (TCP)

# Server configuration (name, owner, password, etc.) is managed in
# data/config/AstroServerSettings.ini — not here. See README.md.

# --- Paths ---
IMAGES_DIR="/var/lib/libvirt/images"
# LOG_DIR="$HOME/.local/log/astroneer-vps"  # optional, defaults to ~/.local/log/astroneer-vps
